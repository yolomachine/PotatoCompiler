#include <iostream>
#include "LexicalAnalyzer.hpp"

int main(int argc, char* argv[]) {
    if (argc < 2) {
        std::cout << "\n\tPascal Compiler\n\tAlexander Gomeniuk, 2017\n";
    }

    getc(stdin);
    getc(stdin);
}